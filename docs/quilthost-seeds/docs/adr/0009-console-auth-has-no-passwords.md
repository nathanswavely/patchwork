# Console auth has no passwords

The Quilthost console authenticates the way Patchwork does: magic link
to sign up, passkey for return visits, zero passwords — values
consistency, and one less secret store to breach. Stripe holds the
cards; Quilthost holds nothing worth stealing beyond session cookies.
